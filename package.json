{
  "name": "twsg-register-arrival.github.io",
  "version": "1.0.0",
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "test": "npm run test:flows && npm run test:screenshot",
    "test:flows": "npx mocha tests/flows.js --timeout 15000",
    "test:screenshot": "npx mocha tests/screenshot.js",
    "lint": "npx eslint --ext .js,.html ."
  },
  "devDependencies": {
    "eslint": "^6.8.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-html": "^6.0.0",
    "mocha": "^7.0.1",
    "taiko": "^1.0.3"
  }
}
